import { Lato, Lustria } from 'next/font/google';

export const lato = Lato({
  subsets: ['latin'],
  weight: ['100', '300', '400'],
});

export const lustria = Lustria({
  subsets: ['latin'],
  weight: ['400'],
});
